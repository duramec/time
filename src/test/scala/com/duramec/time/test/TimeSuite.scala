package com.duramec.time.test

import org.scalatest.FunSuite

class TimeSuite extends FunSuite {

  /*
  test ("can convert a UTC Instant to an Instant") {
    fail()
  }
  
  test ("can convert a GPS Instant to an Instant") {
    fail()
  }
  
  test ("can convert a TAI Instant to an Instant") {
    fail()
  }
  
  test ("can convert between a UTC Instant and a GPS Instant") {
    fail()
  }
  
  test ("can convert between a GPS Instant and a TAI Instant") {
    fail()
  }
  
  test ("can convert between a UTC Instant and a TAI Instant") {
    fail()
  }
  
  test ("can compare a UTC Instant and a GPS Instant") {
    fail()
  }
  
  test ("can compare a GPS Instant and a TAI Instant") {
    fail()
  }
  
  test ("can compare a UTC Instant and a TAI Instant") {
    fail()
  }
  
  test ("TAI is correctly offset from the GPS by 19 seconds") {
    fail()
  }
  
*/

}